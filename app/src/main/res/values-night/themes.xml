<resources>
    <!-- Dark mode override for Theme.ScanSafe -->
    <style name="Theme.ScanSafe" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>